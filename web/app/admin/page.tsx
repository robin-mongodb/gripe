export default function AdminPage() {
  return (
    <section>
      <h1 className="text-2xl font-semibold">Admin console</h1>
      <p className="mt-2 text-sm text-neutral-600">
        Every merchant, every payment. Wired up once the API is live.
      </p>
    </section>
  );
}
